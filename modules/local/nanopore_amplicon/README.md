## Amplicon Variant Caller Info

The parameters match those for shotgun data for `medaka` and `nanopolish`, `clair3` has some slightly different ones

The main difference is that the amplicon processes are heavily coupled to use of the readgroup/bed_file/regions
settings of the different tools while the shotgun processes are not
